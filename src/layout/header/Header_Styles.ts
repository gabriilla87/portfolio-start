import styled from "styled-components";

const Header = styled.header`
    padding: 12px 0;
    top: 0;
    left: 0;
    right: 0;
    background-color: transparent;
    position: fixed;
    z-index: 99999;
`

export const S = {
    Header
}