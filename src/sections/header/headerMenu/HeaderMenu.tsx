import React from 'react';
import styled from "styled-components";

const HeaderMenu = () => {
    return (
        <StyledHeaderMenu>

        </StyledHeaderMenu>
    );
};

const StyledHeaderMenu = styled.nav`
    
`